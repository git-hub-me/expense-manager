# iOS Configuration

# To use the app on your iOS device:
# 1. Find your Mac's IP address: System Settings > Network > Wi-Fi > Details
# 2. Uncomment the line below and replace with your Mac's IP
# 3. Rebuild: npm run build && npx cap sync
# 4. Make sure your backend is running on port 3000

# VITE_API_URL=http://192.168.1.100:3000

# For web development, leave this empty (uses relative paths with Vite proxy)
